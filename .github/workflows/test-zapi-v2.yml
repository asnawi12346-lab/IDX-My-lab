```yaml
name: Test Zapi V2

on:
  workflow_dispatch:

jobs:
  test-zapi:
    runs-on: ubuntu-latest

    steps:
      - name: Test Zapi
        env:
          ZAPI_API_KEY: ${{ secrets.ZAPI_API_KEY }}
        run: |
          echo "================================"
          echo "TEST ZAPI"
          echo "================================"

          echo ""
          echo "1. STOCK SUMMARY"
          curl -sS \
            -H "x-api-key: ${ZAPI_API_KEY}" \
            "https://api.zpi.web.id/v1/finance:idx/stock-summary?code=BBRI"

          echo ""
          echo ""
          echo "2. BROKER SUMMARY"
          curl -sS \
            -H "x-api-key: ${ZAPI_API_KEY}" \
            "https://api.zpi.web.id/v1/finance:idx/broker-summary?code=BBRI"

          echo ""
          echo ""
          echo "3. FOREIGN FLOW"
          curl -sS \
            -H "x-api-key: ${ZAPI_API_KEY}" \
            "https://api.zpi.web.id/v1/finance:idx/foreign-flow?code=BBRI"

          echo ""
          echo ""
          echo "================================"
          echo "TEST SELESAI"
          echo "================================"
```
