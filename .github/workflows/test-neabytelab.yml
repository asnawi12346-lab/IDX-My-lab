name: Test NeaByteLab IDX-API

on: [workflow_dispatch]

jobs:
  test-neabytelab:
    runs-on: ubuntu-latest

    steps:
      - name: Verify workflow
        run: echo "NeaByteLab workflow is registered and runnable"
